import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `Select an element and click submit to load into browser.`,
  styles: []
})
export class AppComponent {
  title = 'angular-dynamic-loader';
}
